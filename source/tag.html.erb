---
title: Category
---
<div class="row">
  <div class="columns">
    <h2 class="tag">All articles listed as: <i><%= tagname %></i></h2>
  </div>
</div>
<div class="row align-center">
  <div class="columns">
    <!-- List Articles -->
    <div class="row">
      <div class="columns">
        <% page_articles.each_with_index do |article| %>
        <article>
          <img src="<%= article.data.thumbnail_image %>">
          <h2>
            <a href="<%= article.url %>"><%= article.title %></a>
          </h2>
          <time><%= article.date.strftime('%b %e, %Y') %></time>
          <%= article.summary %>
          <div class="row align-middle align-justify">
            <div class="shrink columns"><a href="<%= article.url %>" class="hollow button">Read more</a></div>

            <div class="shrink columns">
              <div class="sharing">
                <a class="sharer social-button facebook" data-sharer="facebook" data-url="<%= article.url %>">
                  <span class="ion-social-facebook"></span>
                </a>
                <a class="sharer social-button twitter" data-sharer="twitter" data-title="<%= article.title %>" data-hashtags="<%= article.data.hashtags %>" data-url="<%= article.url %>">
                  <span class="ion-social-twitter"></span>
                </a>
                <a class="sharer social-button whatsapp" data-sharer="whatsapp" data-title="<%= article.title %>"" data-url="<%= article.url %>">
                  <span class="ion-social-whatsapp"></span>
                </a>
                <a class="sharer social-button email" data-sharer="email" data-title="<%= article.title %>"" data-url="<%= article.url %>" data-subject="Hey! Check out that URL" data-to="some@email.com">
                  <span class="ionicons ion-at"></span>
                </a>
              </div>
            </div>
          </div>
          <div class="row border-top">
            <div class="columns">
              Category:
              <% article.tags.each do |tag| %>
              <span class="label"><%= tag %></span>
              <% end %>
            </div>
          </div>
        </article>
        <% end %>
      </div>
    </div>
  </div>
  <div class="small-12 medium-3 columns categories">
    <% blog.tags.each do |tag, articles| %>
    <ul>
      <li><%= link_to "#{tag} (#{articles.size})", tag_path(tag, 'blog') %></li>
    </ul>
    <% end %>
  </div>
</div>
